<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>
<?import javafx.scene.shape.Rectangle?>

<StackPane xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="gradle.ArenaSelectionController">
   <Rectangle fill="transparent" height="800.0" width="1200.0" styleClass="animated-background" />
   
   <VBox alignment="CENTER" spacing="40.0" styleClass="main-container">
      <padding>
         <Insets bottom="40.0" left="40.0" right="40.0" top="40.0" />
      </padding>
      
      <VBox alignment="CENTER" spacing="20.0">
         <Text text="🏟️ SELEÇÃO DE ARENA" styleClass="title-primary" />
         <Label text="Escolha o campo de batalha" styleClass="subtitle" />
         <Button text="← VOLTAR" styleClass="nav-button" onAction="#handleBack" />
      </VBox>
      
      <HBox alignment="CENTER" spacing="40.0">
         <VBox alignment="CENTER" spacing="20.0" styleClass="dark-card" prefWidth="300.0">
            <Label text="🏜️ DESERTO" styleClass="title-secondary" />
            <Label text="Terreno árido com visibilidade reduzida" styleClass="subtitle" textAlignment="CENTER" wrapText="true" />
            <Button text="SELECIONAR" styleClass="modern-button" onAction="#handleDesertArena" />
         </VBox>
         
         <VBox alignment="CENTER" spacing="20.0" styleClass="dark-card" prefWidth="300.0">
            <Label text="🏙️ URBANO" styleClass="title-secondary" />
            <Label text="Ambiente urbano com obstáculos" styleClass="subtitle" textAlignment="CENTER" wrapText="true" />
            <Button text="SELECIONAR" styleClass="modern-button" onAction="#handleUrbanArena" />
         </VBox>
         
         <VBox alignment="CENTER" spacing="20.0" styleClass="dark-card" prefWidth="300.0">
            <Label text="🌾 CAMPO ABERTO" styleClass="title-secondary" />
            <Label text="Terreno plano ideal para combate" styleClass="subtitle" textAlignment="CENTER" wrapText="true" />
            <Button text="SELECIONAR" styleClass="modern-button" onAction="#handleOpenFieldArena" />
         </VBox>
      </HBox>
   </VBox>
</StackPane>